name: Tiny Tapeout CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Tiny Tapeout Validation
        uses: tinytapeout/tt-ci-action@v1
